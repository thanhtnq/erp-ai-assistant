# ── Web server ────────────────────────────────────────────────────────────────
fastapi
uvicorn[standard]       # ASGI server — run with: uvicorn api:app --reload
aiofiles                # required by FastAPI StaticFiles
python-multipart        # required by FastAPI Form/File upload routes

# ── Data validation ───────────────────────────────────────────────────────────
pydantic

# ── LLM (Gemini API) ──────────────────────────────────────────────────────────
google-genai

# ── Vector DB & Reranker ──────────────────────────────────────────────────────
chromadb
sentence-transformers   # CrossEncoder reranker (ms-marco-MiniLM-L-6-v2)

# ── Document parsing ──────────────────────────────────────────────────────────
markitdown[docx,pdf]    # DOCX/PDF → Markdown (Microsoft); pulls in mammoth
python-docx             # .docx files (also used by markitdown for image extraction)
pdfplumber              # PDF text extraction
PyMuPDF                 # PDF image extraction (imported as fitz)
lxml                    # XML/HTML parsing (debug tools)

# ── Database ──────────────────────────────────────────────────────────────────
psycopg2-binary         # PostgreSQL (ticket ingestion — optional if not using PG)

# ── Utilities ─────────────────────────────────────────────────────────────────
tqdm                    # progress bars
schedule                # background job scheduler (schedule/scheduler.py)
python-dotenv           # load .env file into os.environ

# SCM training / analytics
pandas                  # processed SCM datasets
numpy                   # feature engineering
sqlalchemy              # PostgreSQL extraction via pandas
scikit-learn            # churn prediction, sales forecast, segmentation
scipy                   # product trend scoring
pyarrow                 # parquet files
openpyxl                # optional Excel export/import
colorlog                # optional colored training logs